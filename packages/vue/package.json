{
  "name": "@device-frame/vue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint ."
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/compiler-sfc": "^3.5.16", // Should be kept for SFC compilation if not handled by vite alone
    "@vue/tsconfig": "^0.7.0",
    "eslint-plugin-vue": "^10.2.0", // Added in this subtask
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "vue": "^3.5.16", // Added to devDependencies
    "vue-tsc": "^2.2.8" // For type checking, often used with `vite build` or separately
  },
  "peerDependencies": {
    "vue": "^3.5.0" // Added as per subtask note
  }
}
