export interface DeviceType {
  // Add properties for DeviceType based on existing understanding or requirements
  // For example:
  id: string;
  name: string;
  model: string;
}

export interface DeviceConfig {
  // Add properties for DeviceConfig based on existing understanding or requirements
  // For example:
  deviceId: string;
  settings: Record<string, any>;
}
