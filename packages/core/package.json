{
  "name": "@device-frame/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "vite build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.15.30",
    "tsup": "^8.0.0"
    // Note: tsup might be replaced by vite if "vite build" is the new build command.
    // The subtask didn't specify removing/adding devDependencies other than ESLint related ones (which are root level).
    // For now, I'll keep existing devDependencies. If 'vite' is needed, it should be added.
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  }
}
